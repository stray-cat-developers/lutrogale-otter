{
  "name": "lutrogale-otter-e2e",
  "version": "1.0.0",
  "scripts": {
    "test": "playwright test",
    "test:ui": "playwright test --ui",
    "test:debug": "playwright test --debug",
    "report": "playwright show-report",
    "codegen": "playwright codegen http://localhost:4211"
  },
  "devDependencies": {
    "@playwright/test": "^1.44.0"
  }
}
